---
fixes:
  - |
    Fixes RabbitMQ precheck failing with "No TLS certificate/key provided
    for RabbitMQ" when relying on the shared ``kolla_tls_backend_cert``/
    ``kolla_tls_backend_key`` fallback, which ``service-cert-copy`` already
    supports but the precheck did not check for.
