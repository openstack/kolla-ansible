---
fixes:
  - |
    Fixes image check with containers that don't exist locally. A bug in the
    image comparison logic would make service-check-containers fail with
    ``AttributeError: NoneType object has no attribute get``
    `LP#2166024  <https://launchpad.net/bugs/2166024>`__
