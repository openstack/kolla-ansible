---
fixes:
  - |
    Fixes an issue in the Docker container worker where reconfiguring
    containers with ``cpuset_cpus`` or ``cpuset_mems`` dimensions could fail
    with an ``IndexError`` when the existing container had no explicit cpuset
    constraint configured.
