---
upgrade:
  - |
    By default, ML2/OVS L3 routers no longer set
    ``ha_vrrp_health_check_interval`` when
    ``enable_neutron_agent_ha`` is true.
    This is to prevent stability issues when
    Neutron is unable to keep up with L3 router state changes.
