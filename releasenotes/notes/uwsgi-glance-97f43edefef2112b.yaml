---
upgrade:
  - |
    ``glance-api`` is now running under uWSGI and supports backend TLS without
    the additional haproxy container.
    The ``glance-tls-proxy`` container will be removed during the upgrade
    process.
