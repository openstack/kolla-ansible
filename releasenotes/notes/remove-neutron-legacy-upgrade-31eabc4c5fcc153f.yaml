---
upgrade:
  - The legacy upgrade method for Neutron has been removed in favour of the
    rolling upgrade which has been the default since Ocata.
    ``neutron_enable_rolling_upgrade`` should no longer be set.
