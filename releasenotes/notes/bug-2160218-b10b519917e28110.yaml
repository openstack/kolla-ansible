---
fixes:
  - |
    This resolves an issue in Glance multinode deployments with internal TLS
    enabled. Some concurrent image operations, such as deleting an image while
    it is actively uploading or staging, could fail with an HTTP 500 Internal
    Server Error.
    `LP#2160218 <https://launchpad.net/bugs/2160218>`__
