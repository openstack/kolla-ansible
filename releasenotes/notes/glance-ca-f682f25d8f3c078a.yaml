---
fixes:
  - |
    Fixed an issue in Glance where enabling
    ``kolla_copy_ca_into_containers`` forced a check for missing service
    certificates. The ``glance-api`` container now only requires
    ``glance-cert.pem`` if ``glance_enable_tls_backend`` is explicitly
    set to ``yes``.
