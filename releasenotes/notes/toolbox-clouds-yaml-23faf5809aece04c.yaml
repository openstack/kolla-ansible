---
upgrade:
  - |
    ``kolla-toolbox`` and all kolla_toolbox based invocations of Ansible
    openstack.cloud modules have been reworked to use clouds.yaml.
    ``openstack_auth`` variable is only used for passing keystone admin
    password for services bootstrap runs.
