---
features:
  - |
    Adds warning and support for automatic pruning of stale Kibana indices
    during OpenSearch migration by running
    ``kolla-ansible opensearch-migration`` with ``--prune-kibana-indices``
    parameter.
