---
upgrade:
  - |
    ``ovn`` role has been split into ``ovn-controller`` and ``ovn-db``
    roles, ``ovn_extra_volumes`` will still be used, but users will
    need to adapt their config to use ``ovn_db_extra_volumes`` or
    ``ovn_controller_extra_volumes`` when upgrading to Zed.
