---
upgrade:
  - |
    Changes the default of ``mariadb_clustercheck_tag`` and ``mariabackup_tag``
    from ``openstack_tag`` to ``mariadb_tag``. This allows one variable to set
    the tag for all MariaDB images.
