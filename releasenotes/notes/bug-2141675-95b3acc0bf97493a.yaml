---
fixes:
  - |
    Fixed race conditions during configuration validation. Tasks
    involving container facts retrieval and ``exec`` commands now
    include a retry mechanism to handle cases where the container
    runtime is not immediately ready.
    `LP#2141675 <https://launchpad.net/bugs/2141675>`__
