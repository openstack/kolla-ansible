---
upgrade:
  - |
    Glance deployment now uses Multi-Store support. Users that have
    ``default_stores`` in their service config overrides for
    ``glance-api.conf`` should remove it and use ``default_backend`` if needed.
