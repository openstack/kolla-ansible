---
features:
  - |
    Removed obsolete configuration options from the Horizon
    ``local_settings.py`` template:

    - ``TEMPLATE_DEBUG``: No longer supported
    - ``enable_lb``, ``enable_firewall``, and ``enable_vpn``: These
      keys inside ``OPENSTACK_NEUTRON_NETWORK`` are now invalid.
