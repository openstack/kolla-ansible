---
fixes:
  - |
    Fixed race conditions during RabbitMQ restarts. Tasks involving
    ``exec`` commands now include a retry mechanism to handle cases
    where the container runtime is not immediately ready. This
    addresses intermittent "container state improper" and connection
    reset errors.
    `LP#2141675 <https://launchpad.net/bugs/2141675>`__
