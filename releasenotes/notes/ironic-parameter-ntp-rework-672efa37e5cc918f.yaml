---
features:
  - |
    Adds explicit support for passing through the ``ipa-ntp-server`` setting
    via the kernel commandline to the Ironic Python Agent.

    You can now also use ``ironic_kernel_append_params`` to provide
    additional arguments to the kernel command line when booting the Ironic
    Python Agent.
