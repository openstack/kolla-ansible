---
features:
  - |
    Adds support for LDAP authentication in Grafana.
    Users can now enable LDAP by setting ``grafana_ldap_enabled`` to
    ``true`` and providing an ``ldap.toml`` configuration file in the
    ``{{ node_custom_config }}/grafana/`` directory.
