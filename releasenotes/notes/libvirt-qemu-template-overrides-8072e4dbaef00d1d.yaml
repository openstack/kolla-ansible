---
features:
  - |
    The ``nova-cell`` role now supports operator-specified custom templates
    for ``qemu.conf`` and ``libvirtd.conf``. Kolla-Ansible will now look for
    host-specific and global overrides before falling back to the default
    templates.
