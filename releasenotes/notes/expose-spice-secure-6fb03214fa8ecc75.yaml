---
other:
  - |
    This release exposes additional configuration options for OpenStack
    Compute consoles which utilize the SPICE protocol. You can now require
    that such consoles use channels secured by TLS using the
    ``nova_spice_require_secure`` configuration option. This option defaults
    to ``false`` as it implies that TLS certificates have been distributed
    to your ``nova-compute`` nodes.
