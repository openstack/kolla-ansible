---
- name: Register gnocchi in Keystone
  ansible.builtin.import_tasks: register.yml

- name: Configure gnocchi
  ansible.builtin.import_tasks: config.yml

- name: Check gnocchi containers
  ansible.builtin.import_tasks: check-containers.yml

- name: Bootstrap gnocchi service
  ansible.builtin.import_tasks: bootstrap.yml

- name: Flush handlers
  ansible.builtin.meta: flush_handlers
