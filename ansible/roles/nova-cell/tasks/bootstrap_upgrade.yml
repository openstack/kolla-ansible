---
# For upgrade, we need to apply DB schema migrations to the cell databases.

- name: Import tasks from config_bootstrap.yml
  ansible.builtin.import_tasks: config_bootstrap.yml
- name: Import tasks from bootstrap_service.yml
  ansible.builtin.import_tasks: bootstrap_service.yml
