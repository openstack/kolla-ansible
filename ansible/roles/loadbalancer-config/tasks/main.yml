---
- name: Import haproxy-config role
  import_role:
    name: haproxy-config
  when: haproxy_config_install | bool

- name: Import proxysql-config role
  import_role:
    name: proxysql-config
  when: enable_proxysql | bool
