---
- name: Register ceilometer service, endpoints, and users in Keystone
  import_role:
    name: service-ks-register
  vars:
    service_ks_register_auth: "{{ openstack_ceilometer_auth }}"
    service_ks_register_users: "{{ ceilometer_ks_users }}"
