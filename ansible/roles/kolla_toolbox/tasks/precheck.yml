---
- name: Precheck kolla_toolbox configuration
  ansible.builtin.import_role:
    name: service-precheck
  vars:
    service_precheck_services: "{{ kolla_toolbox_services }}"
    service_name: "{{ project_name }}"
