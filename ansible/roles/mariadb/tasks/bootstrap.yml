---
- name: Import tasks from lookup_cluster.yml
  ansible.builtin.import_tasks: lookup_cluster.yml

- name: Include tasks from bootstrap_cluster.yml
  ansible.builtin.include_tasks: bootstrap_cluster.yml
  when:
    - not mariadb_cluster_exists
    - inventory_hostname == groups[mariadb_shard_group][0]

- name: Include tasks from recover_cluster.yml
  ansible.builtin.include_tasks: recover_cluster.yml
  when: mariadb_recover | default(False)
