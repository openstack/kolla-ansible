---
- name: Remove mariadb-clustercheck
  become: true
  kolla_container:
    action: "stop_and_remove_container"
    common_options: "{{ docker_common_options }}"
    name: "mariadb_clustercheck"
    ignore_missing: true
  when: mariadb_loadbalancer == 'proxysql'

- import_tasks: deploy.yml
