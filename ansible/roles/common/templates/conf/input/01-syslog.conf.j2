<source>
  @type syslog
  port {{ fluentd_syslog_port }}
{% if orchestration_engine != 'KUBERNETES' %}
  bind {{ api_interface_address }}
{% else %}
  bind 0.0.0.0
{% endif %}
  tag syslog
  format /^(?<Payload>.*)$/
</source>
