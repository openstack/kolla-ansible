---
- name: Include tasks from lookup_cluster.yml
  ansible.builtin.include_tasks: lookup_cluster.yml
  when:
    - inventory_hostname in groups['ovn-nb-db'] or
      inventory_hostname in groups['ovn-sb-db']

- name: Include tasks from bootstrap-initial.yml
  ansible.builtin.include_tasks: bootstrap-initial.yml
  when:
    - inventory_hostname in groups.get('ovn-nb-db_had_volume_False', '') or
      inventory_hostname in groups.get('ovn-sb-db_had_volume_False', '')

- name: Configure ovn-db
  ansible.builtin.import_tasks: config.yml

- name: Check ovn-db containers
  ansible.builtin.import_tasks: check-containers.yml

- name: Flush handlers
  ansible.builtin.meta: flush_handlers

- name: Import tasks from bootstrap-db.yml
  ansible.builtin.import_tasks: bootstrap-db.yml
  when:
    - inventory_hostname in groups['ovn-nb-db']
    - inventory_hostname in groups['ovn-sb-db']
